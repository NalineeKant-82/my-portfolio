import { Box, Typography } from "@mui/material";

const Resume = () => {
  return (
    <Box>
      <Typography sx={{ backgroundColor: "white" }}>Resume</Typography>
    </Box>
  );
};
export default Resume;
